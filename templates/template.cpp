#include "bits/stdc++.h"
#define rep(i,n) for(int i=0;i<(n);++i)
#define rep1(i,p,q) for(int i=(p);i<=(q);++i)
#define veci vector<int>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
}

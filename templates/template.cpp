#include "bits/stdc++.h"

// Loops
#define rep(i,n) for(int i=0;i<(n);++i)
#define rep1(i,p,q) for(int i=(p);i<=(q);++i)

// Vector macros
#define vi vector<int>
#define visum(v) accumulate((v).begin(),(v).end(),0)
#define vsort(v) sort((v).begin(),(v).end())
#define vsortr(v) sort((v).rbegin(),(v).rend())

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
}
